apiVersion: v1
kind: Service
metadata:
  name: katalog-manager-ui
  namespace: stube
  labels:
    app: katalog-manager-ui
spec:
  type: ClusterIP
  selector:
    app: katalog-manager-ui
  ports:
    - name: http
      port: 80
      targetPort: 8080
