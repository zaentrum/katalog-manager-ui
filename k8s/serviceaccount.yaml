apiVersion: v1
kind: ServiceAccount
metadata:
  name: katalog-manager-ui
  namespace: stube
  labels:
    app: katalog-manager-ui
    value-stream: stube
# If your image lives in a private registry, link an image-pull secret
# to this ServiceAccount so the deployment can pull it.
